/**
 * Placeholder top-of-page control bar. Currently renders only a static, non-functional
 * "Play" button.
 */

/**
 * Renders the (currently placeholder) main control bar.
 */
export default function MainBar() {
  return (
    <>
      <div className="controls">
        <button>Play</button>
      </div>
    </>
  )
}
